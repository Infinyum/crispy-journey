apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crispy
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - secretName: crispy-local-tls
    hosts:
    - crispy.local
  rules:
  - host: crispy.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: crispy
            port:
              number: 80
